[project]
name = "fastapi-examples"
version = "0.1.0"
description = "Runnable FastAPI examples, including a basic endpoint and LLM model judging."
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "fastapi>=0.115",
    "openai>=1.0",
    "pydantic>=2.0",
    "python-dotenv>=1.0",
    "uvicorn[standard]>=0.30",
]
